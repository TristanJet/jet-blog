This project is an attempt to create a minimal single-file script to generate a static blog from markdown files.

Logic should be in index.ts but avoid putting markup in the script file.

content/ is where the content markdown files should be.

layouts/ should be where the markup lives, inject data using these layouts.

public/ is where the final generated website should be.
